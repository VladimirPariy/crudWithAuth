import {Router} from 'express';import {check} from 'express-validator'import AuthController from "./AuthController.js";const authRouter = new Router()const checkByEmail = (field, msg) => check(field, msg).isEmail()const checkByEmpty = (field, msg) => check(field, msg).notEmpty()const checkByLength = (field, msg, min = 1, max = 20) => check(field, msg).isLength({max: max, min: min})authRouter.post('/registration', [  checkByEmpty('login', 'Имя не должно быть пустым'),  checkByLength('password', 'Пароль должен быть не более 10 и не менее 3 символов', 3, 10),  checkByEmail('email', 'Email не соответствует установленные параметрам')], AuthController.registration)authRouter.post('/login',  AuthController.login)export {authRouter};