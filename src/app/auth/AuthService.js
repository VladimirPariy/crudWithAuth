import {knexInstance} from "../../DB/knexConfig.js";class AuthService {  async getUserByNameOrEmail(login = '', email = '') {    return knexInstance('users').where("login", login).orWhere('email', email)  }  async registration(login, email, hashPass) {    return knexInstance('users').insert([{'login': login, 'email': email, 'password': hashPass}])  }}export default new AuthService();