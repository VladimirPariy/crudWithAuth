import {UsersModel} from "../../DB/models/usersModel.js";class AuthService {  async getUserByNameOrEmail(login = '', email = '') {    return UsersModel.query()      .where({login})      .orWhere({email});  }  async registration(login, email, hashPass) {    return UsersModel.query()      .insert({login, email, password:hashPass});  }}export default new AuthService();