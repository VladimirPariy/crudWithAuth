import {Router} from 'express';import UsersController from "./users.controller";import {authorizationCheck} from "../middleware/auth.middleware";const usersRouter = Router();usersRouter.get('/users', authorizationCheck, UsersController.getAllUsers);usersRouter.get('/users/:id', authorizationCheck, UsersController.getOneUser);usersRouter.patch('/users', authorizationCheck, UsersController.updateUser);usersRouter.delete('/users/:id', authorizationCheck, UsersController.deleteOneUser);usersRouter.delete('/users', authorizationCheck, UsersController.deleteAllUsers);export {usersRouter};